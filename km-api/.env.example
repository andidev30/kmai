# Example environment variables for km-api

# PostgreSQL connection string, e.g.
# postgres://user:password@localhost:5432/km_ai
DATABASE_URL=

# Optional CORS origin override (defaults to *)
# CORS_ORIGIN=http://localhost:5173

# API port (defaults to 8787)
# PORT=8787
